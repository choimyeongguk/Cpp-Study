#include <bits/stdc++.h>
using namespace std;

int main() {
	ios_base::sync_with_stdio(0);
	cin.tie(0);
	cout.tie(0);
	
	long long N;
	string str;
	
	cin >> N;
	while(N--) {
		cin >> str;
		cout << ((str.back() - '0') % 2 == 0 ? "even" : "odd") << "\n";
	}
	
	return 0;
}