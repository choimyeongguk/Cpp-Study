#include <bits/stdc++.h>
using namespace std;

int main() {
	int uk, je;
	long double M;
	cin >> uk >> je;
	M = (long double)(je - uk) / 400;
	cout << fixed << setprecision(10) << 1 / (1 + pow(10, M));
} 